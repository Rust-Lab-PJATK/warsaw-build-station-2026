# Loco configuration file documentation

# Application logging configuration
logger:
  # Enable or disable logging.
  enable: true
  # Enable pretty backtrace (sets RUST_BACKTRACE=1)
  pretty_backtrace: true
  # Log level, options: trace, debug, info, warn or error.
  level: debug
  # Define the logging format. options: compact, pretty or json
  format: compact
  # By default the logger has filtering only logs that came from your code or logs that came from `loco` framework. to see all third party libraries
  # Uncomment the line below to override to see all third party libraries you can enable this config and override the logger filters.
  # override_filter: trace

# Web server configuration
server:
  # Port on which the server will listen. the server binding is 0.0.0.0:{PORT}
  port: 5150
  # Binding for the server (which interface to bind to)
  binding: 0.0.0.0
  # The UI hostname or IP address that mailers will point to.
  host: http://localhost
  # Open AI configuration

  # Out of the box middleware configuration. to disable middleware you can changed the `enable` field to `false` of comment the middleware block
  middlewares:

# Worker Configuration
workers:
  # specifies the worker mode. Options:
  #   - BackgroundQueue - Workers operate asynchronously in the background, processing queued.
  #   - ForegroundBlocking - Workers operate in the foreground and block until tasks are completed.
  #   - BackgroundAsync - Workers operate asynchronously in the background, processing tasks with async capabilities.
  mode: BackgroundAsync

# Initializers Configuration
# initializers:
#  oauth2:
#    authorization_code: # Authorization code grant type
#      - client_identifier: google # Identifier for the OAuth2 provider. Replace 'google' with your provider's name if different, must be unique within the oauth2 config.
#        ... other fields

settings:
  estimate_provider: {{ get_env(name="ESTIMATE_PROVIDER") }}
  elevenlabs_api_key: {{ get_env(name="ELEVENLABS_API_KEY") }}
  elevenlabs_agent_id: {{ get_env(name="ELEVENLABS_AGENT_ID") }}
  elevenlabs_base_url: {{ get_env(name="ELEVENLABS_BASE_URL") }}
  elevenlabs_conversation_ws_url: {{ get_env(name="ELEVENLABS_CONVERSATION_WS_URL", default="wss://api.elevenlabs.io/v1/convai/conversation") }}
  elevenlabs_conversation_timeout_secs: {{ get_env(name="ELEVENLABS_CONVERSATION_TIMEOUT_SECS", default="600") }}
  elevenlabs_simulation_turns_limit: {{ get_env(name="ELEVENLABS_SIMULATION_TURNS_LIMIT") }}
  elevenlabs_simulated_user_language: {{ get_env(name="ELEVENLABS_SIMULATED_USER_LANGUAGE", default="en") }}
  elevenlabs_simulated_user_prompt: {{ get_env(name="ELEVENLABS_SIMULATED_USER_PROMPT", default="") }}
  elevenlabs_simulated_user_llm: {{ get_env(name="ELEVENLABS_SIMULATED_USER_LLM", default="") }}
  elevenlabs_use_backend_prompt: {{ get_env(name="ELEVENLABS_USE_BACKEND_PROMPT", default="false") }}
  elevenlabs_rag_parent_folder_id: {{ get_env(name="ELEVENLABS_RAG_PARENT_FOLDER_ID", default="") }}
  elevenlabs_rag_upsert_url: {{ get_env(name="ELEVENLABS_RAG_UPSERT_URL", default="") }}
