 [package]
  name = "ochain-marketplace"
  version = "0.1.0"
  edition = "2021"

  [lib]
  crate-type = ["cdylib", "lib"]
  name = "ochain_marketplace"

  [features]
  no-entrypoint = []
  cpi = ["no-entrypoint"]

  [dependencies]
  anchor-lang = "0.30.1"

  [profile.release]
  overflow-checks = true
  lto = "fat"
  codegen-units = 1
